@model E_Nompilo_Healthcare_system.Models.Referrals

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="bg-primary">
    <h1 class="text-center text-white">Delete Referrals</h1>
</div>

<h3 class="mt-3">Are you sure you want to delete this?</h3>
<div class="p-3 border border-secondary">
    <h4>Referrals</h4>
    <hr />
   @*  <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(Model => Model.Patient)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Patient)
        </dd>
    </dl> *@

    <form asp-action="Delete">
        <input type="hidden" asp-for="ID" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
