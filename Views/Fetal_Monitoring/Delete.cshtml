@model E_Nompilo_Healthcare_system.Models.Fetal_Monitoring

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_PreNatalLayout.cshtml";
}

<div class="bg-primary p-3 mb-2">
    <h4 class="text-white text-center">Delete Fetal Monitoring</h4>
</div>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Fetal Monitoring</h4>
    <hr />
   @*  <dl class="row">

        <dt class="col-sm-2">Date</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Date)</dd>

        <dt class="col-sm-2">MainUser</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.MainUser.Id)</dd>

        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Status)</dd>

        <dt class="col-sm-2">Total</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Total)</dd>

    </dl> *@

    

    <form asp-action="Delete">
        <input type="hidden" asp-for="FetalMonitorID" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-primary">Back to List</a>
    </form>
</div>
